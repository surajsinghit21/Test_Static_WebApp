name: Hello World

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print message for build
      run: echo "This is Buid-Hello-World"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Print message for test
      run: echo "This is Test-Hello-World"

